import React from 'react';
import './ShowProgress.css'; // Assume you have some CSS for styling

const ShowProgress = () => {
  return (
    <div className="show-progress-container">
      <h1>Show Progress</h1>
      <p>View the progress of the pen test.</p>
      {/* You can add more functionality like progress bars, status indicators, etc. */}
    </div>
  );
};

export default ShowProgress;
