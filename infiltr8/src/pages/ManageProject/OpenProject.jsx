import './openProject.css'; // Adjust the path as necessary

const OpenProject = () => {
    // If you need to handle events or state, add them here

    return (
        <main className="table" id="customers_table">
            {/* The rest of your transformed JSX */}
        </main>
    );
};

export default OpenProject;
